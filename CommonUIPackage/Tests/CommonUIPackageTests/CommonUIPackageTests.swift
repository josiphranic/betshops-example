    import XCTest
    @testable import CommonUIPackage

    final class CommonUIPackageTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(CommonUIPackage().text, "Hello, World!")
        }
    }
